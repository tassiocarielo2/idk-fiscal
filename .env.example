# Supabase — projeto IDK Fiscal
# Pegue em: https://supabase.com/dashboard/project/vejpzbqsysxbfpqpkbto/settings/api
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Apenas server-side. NUNCA exponha no client.
SUPABASE_SERVICE_ROLE_KEY=

# Base URL da app — usado para construir links de convite (/invite/[token]).
# Ex.: http://localhost:3000 (dev) ou https://app.idkfiscal.com.br (prod).
NEXT_PUBLIC_APP_URL=

# Personal Access Token do Supabase — usado pelo MCP server (.mcp.json).
# Gere em: https://supabase.com/dashboard/account/tokens
# Defina no shell antes de iniciar o Claude Code:
#   PowerShell: $env:SUPABASE_ACCESS_TOKEN="sbp_..."
#   bash:       export SUPABASE_ACCESS_TOKEN=sbp_...
SUPABASE_ACCESS_TOKEN=

# GitHub Personal Access Token — usado para abrir/mergear PRs via API quando
# o gh CLI nao esta instalado. Scope minimo: repo (private repositories).
# Gere em: https://github.com/settings/tokens (classic) ou
#          https://github.com/settings/tokens?type=beta (fine-grained)
GITHUB_TOKEN=
