# packages/

Bibliotecas compartilhadas entre apps.

A criar conforme as sessões avançam:

- `domain-fiscal/` — modelagem de domínio fiscal (NF-e, NFS-e, NCM, CFOP, regras tributárias)
- `ui/` — componentes UI compartilhados
- `shared/` — utilitários comuns (formatação, validação, types)

Vazio por enquanto — esta pasta existe pra fixar a estrutura no Git.
